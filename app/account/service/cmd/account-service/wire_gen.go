// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//+build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"github.com/lalifeier/vgo/app/account/service/internal/biz"
	"github.com/lalifeier/vgo/app/account/service/internal/conf"
	"github.com/lalifeier/vgo/app/account/service/internal/data"
	"github.com/lalifeier/vgo/app/account/service/internal/server"
	"github.com/lalifeier/vgo/app/account/service/internal/service"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(confServer *conf.Server, confData *conf.Data, logger log.Logger, registry *conf.Registry) (*kratos.App, func(), error) {
	dataData, cleanup, err := data.NewData(confData, logger)
	if err != nil {
		return nil, nil, err
	}
	accountUserRepo := data.NewAccountUserRepo(dataData, logger)
	accountUserUsecase := biz.NewAccountUserUsecase(accountUserRepo, logger)
	accountService := service.NewAccountService(accountUserUsecase, logger)

	// accountUserService := service.NewAccountUserService(accountUserUsecase, logger)

	grpcServer := server.NewGRPCServer(confServer, accountService, logger)
	registrar := server.NewConsulCRegister(registry)
	app := newApp(logger, grpcServer, registrar)
	return app, func() {
		cleanup()
	}, nil
}
