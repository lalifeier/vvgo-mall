// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//+build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"github.com/lalifeier/vgo/app/account/interface/internal/biz"
	"github.com/lalifeier/vgo/app/account/interface/internal/conf"
	"github.com/lalifeier/vgo/app/account/interface/internal/data"
	"github.com/lalifeier/vgo/app/account/interface/internal/server"
	"github.com/lalifeier/vgo/app/account/interface/internal/service"
	"go.opentelemetry.io/otel/trace"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(confServer *conf.Server, confData *conf.Data, logger log.Logger, registry *conf.Registry, tracerProvider trace.TracerProvider) (*kratos.App, func(), error) {
	discovery := server.NewDiscovery(registry)
	userClient := data.NewUserServiceClient(logger, discovery, tracerProvider)
	dataData, err := data.NewData(logger, userClient)
	if err != nil {
		return nil, nil, err
	}
	userRepo := data.NewUserRepo(dataData, logger)
	userUsecase := biz.NewUserUsecase(userRepo, logger, userClient)
	userService := service.NewUserService(userUsecase, logger)

	httpServer := server.NewHTTPServer(confServer, logger, tracerProvider, userService)
	grpcServer := server.NewGRPCServer(confServer, logger, tracerProvider, userService)
	registrar := server.NewConsulCRegister(registry)
	app := newApp(logger, httpServer, grpcServer, registrar)
	return app, func() {
	}, nil
}
